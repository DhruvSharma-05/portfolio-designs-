# smartcrop-sharp declares a sharp ^0.32 peer but works fine with our
# sharp 0.35 (it only uses the stable raw-pixel API). Accept the newer
# sharp so `npm install` / `npm ci` (locally and on Vercel) don't ERESOLVE.
legacy-peer-deps=true
