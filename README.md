# Photographer Portfolio

A dark, minimal, type-led single-page portfolio. One near-black base, one
fixed accent colour — a switcher used to let visitors change it, but it was
removed at the client's request, so the page just stays calm and consistent.

Built with **React 19 + Vite + React Router**, enhanced with:

- **React Router** — Home, an About page, and a per-project detail page at
  `/work/:seed`, tied together with an **aperture page transition**: an accent
  "shutter" irises closed over the screen, the route swaps while it's covered,
  then it opens onto the next page. Reimplemented in React (rather than Barba,
  which is built for non-React multi-page sites) via a small `go()` in the shell.
- **Lenis** — smooth (inertia) scrolling that smooths the *native* scroll, so
  `position: sticky` keeps working for the bar and the stacking work cards.
- **GSAP + ScrollTrigger** — scroll-driven reveals, the reading-progress bar,
  the metric count-ups, the **pinned horizontal gallery**, the aperture
  transition, and the magnetic email link. Wired through `@gsap/react`'s
  `useGSAP` hook so every animation and ScrollTrigger is torn down on
  unmount / navigation.
- **Motion (Framer Motion)** — page enter animations, the accent-chip spring
  press, and the testimonial crossfade (`AnimatePresence`).
- **React Three Fiber (Three.js)** — an accent-tinted particle field behind the
  hero name, plus a **WebGL hover-ripple** on the work photos. Both **code-split**
  into a shared Three.js chunk so nothing 3D blocks first paint, both skip
  fetching that chunk at all on touch devices, small viewports, or
  `prefers-reduced-motion`, and both degrade gracefully otherwise (the ripple
  keeps a real `<img>` underneath, so if WebGL or a texture fails the photo
  still shows).

## Structure

```text
src/
  main.jsx          BrowserRouter + mount, self-hosted font imports
  App.jsx           shell: theme, Lenis, progress bar, aperture transition, nav, routes
  ErrorBoundary.jsx app-level error boundary (render errors + failed lazy chunks)
  context.js        AppContext (theme + go())
  data.js           placeholder content + the full CSS + prefersReduced()/heavyVisualsAllowed()
  ui.jsx            TLink (transition link), Logo, Reveal, Metrics/Counter
  HeroCanvas.jsx    code-split R3F particle hero
  DistortImage.jsx  code-split WebGL hover-ripple photo (img fallback underneath)
  pages/Home.jsx        long-scroll home + categorised gallery + horizontal strip
  pages/WorkDetail.jsx  /work/:seed project page (prev/next)
  pages/Photography.jsx /photography — featured slideshow + project stack
  pages/PhotoProject.jsx /photography/:slug — grid + carousel roll
  pages/Design.jsx       /design — web project cards
  pages/DesignProject.jsx /design/:slug
  pages/About.jsx        /about
  pages/Client.jsx        /client, /client/:code — client photo delivery
  pages/Admin.jsx         /admin — client-delivery manager (code-split, not shipped to visitors)
  pages/adminMock.js      dev-only fake backend for /admin with no functions running
  pages/NotFound.jsx      catch-all 404
  photos.manifest.json   generated by the Contentful + Drive sync (committed empty)
scripts/
  sync-contentful.mjs  build-time Contentful → photos pull (see below)
  sync-drive.mjs       build-time Drive → admin-authored project pull (frozen — see "The admin panel")
api/
  auth.js, content.js, library.js, share.js, client.js, download.js, mail.js
    Vercel serverless functions backing /admin and /client
  _lib/   drive.js, auth.js, delivery.js, ratelimit.js, mail.js — shared helpers
public/
  favicon.svg  og.svg  _redirects   (Netlify SPA fallback)
  photos/                           (generated: optimized WebP, git-ignored)
vercel.json                         (Vercel SPA fallback + cache/function config)
```

Every motion effect respects `prefers-reduced-motion`: Lenis is skipped (native
scroll is left untouched), the particle field renders a single static frame,
and the GSAP reveals/parallax fall back to the content's final state.

## Run

```bash
npm install
npm run dev      # local dev server (HMR)
npm run build    # production build → dist/
npm run preview  # preview the production build
npm run lint     # oxlint
```

**Deploy note:** this is a client-routed SPA, so the host must serve
`index.html` for unknown paths (SPA fallback) or refreshing `/work/pf-01`
404s. Netlify/Vercel do this automatically; for a plain static host add a
catch-all rewrite to `/index.html`.

## Photos from Contentful

Real Work/Gallery/Portrait photos are pulled from **Contentful** at **build
time** and baked into the site as optimized WebP — nothing is fetched from
Contentful at runtime, so the live site stays fast and static.
`scripts/sync-contentful.mjs` runs automatically before every `npm run build`
(the `prebuild` script), and on demand via `npm run sync`.

**How it flows**

```text
Contentful "Photo" entries ──sync──▶ public/photos/*.webp  +  src/photos.manifest.json ──▶ data.js
        (client edits)      (build)      (git-ignored)              (committed empty)
```

`src/data.js` reads the manifest: when it's populated the site shows the real
photos; when empty (fresh clone, no credentials) it falls back to seeded
`picsum.photos` placeholders, so the repo always builds and runs.

**One-time setup (Contentful free tier — no card required)**

1. [contentful.com](https://www.contentful.com/) → create a space.
2. **Content model → Add content type** named `Photo` (API identifier `photo`)
   with these fields:
   | Field | Type | Notes |
   | --- | --- | --- |
   | `title` | Short text | caption / Work card title |
   | `collection` | Short text | one of `work`, `gallery`, `portrait` — add a validation dropdown with those three values |
   | `order` | Integer | controls sort within a collection (lower first) |
   | `image` | Media, one file | the photo itself |

   `location`, `year` and `role` were dropped from the model — the Work
   detail page's Location/Capture/Role/Year rows render blank for now
   (see `src/pages/WorkDetail.jsx`). Add the fields back later if you
   want those captions.
3. **Settings → API keys → Add API key.** Copy the **Space ID** and the
   **Content Delivery API - access token**.
4. `cp .env.example .env` and set `CONTENTFUL_SPACE_ID` + `CONTENTFUL_ACCESS_TOKEN`
   (`CONTENTFUL_ENVIRONMENT` defaults to `master`).
5. On Vercel, add those same variables under **Settings → Environment
   Variables**. The existing **Deploy Hook** URL still triggers a rebuild that
   re-pulls Contentful and publishes the changes.
6. Add entries in Contentful's web app, set `collection` + `order`, upload the
   image, hit **Publish**. The technical line (`35mm · f/8 · 1/500`) is still
   read automatically from the uploaded file's EXIF for Work photos.

**Client workflow after setup:** add / edit / unpublish entries in Contentful,
then open the bookmarked Deploy Hook link. New photos are live a minute or two
later.

## The admin panel (`/admin`) — client delivery

`/admin` is **client-delivery only**. It does not manage the public portfolio
(Work/Gallery/Portrait come from Contentful, see above — nothing about them is
editable from `/admin`). What it does: create or reuse a Drive folder for a
finished shoot, share it, and hand the client their code — by WhatsApp
copy-paste or straight email.

**Nothing new to pay for.** The client list is stored as a single `content.json`
file *in his own Drive* — there is no database and no second service. The
serverless functions in `api/` are the only server-side code, and they only run
when someone is signed into the admin.

**No "Publish" button, and none is needed.** Client-delivery lookups
(`api/client.js`, `api/download.js`) read Drive **live, at request time** — not
from the static build — so creating a client, sharing a folder, or sending a
code takes effect immediately, with no rebuild.

### How it flows

```
Viraj shoots + edits, uploads the finished set to Drive (one folder per shoot)
Viraj opens /admin        →  creates/picks the Drive folder, sets the code
Press "Share folder"      →  link-sharing turned on for that one folder
Press "Save"              →  writes the client list back to Drive
Copy or email the code    →  client can download immediately, no rebuild
```

### Looking at the interface first (no setup at all)

```bash
npm run dev          # then open http://localhost:5173/admin
```

`npm run dev` runs Vite only — it does not serve the functions in `api/`. When
the admin sees no backend it drops into **preview mode**: no password, and the
list is filled with fake clients so the whole interface can be clicked
through. An amber banner says so. Nothing is saved, and this only ever happens
in a dev build.

To run the *real* thing locally you need the functions, which means the Vercel
CLI plus a filled-in `.env`:

```bash
npm i -g vercel
npm run dev:api      # = vercel dev, serves / and /api together
```

### One-time setup

1. **Delivery folder root.** In Drive, make one folder to hold shoot folders
   (e.g. `Deliveries/`). Share it with the service-account email as
   **Editor** (needed so `/admin` can create new delivery folders inside it —
   Viewer is not enough). Put its ID in `DRIVE_ROOT_FOLDER_ID`. **Never** set
   this folder itself to "Anyone with the link" — see the warning in
   `.env.example`.
2. **Content file.** Create an empty file named `content.json` in Drive and share
   it with the service account as **Editor**. Put its ID in
   `DRIVE_CONTENT_FILE_ID`.
   *You must create this file yourself* — a service account has no storage quota,
   so it can update a file someone else owns but cannot create one.
3. **Password.** Set `ADMIN_PASSWORD` (long) and `ADMIN_SESSION_SECRET` (random —
   `node -e "console.log(crypto.randomUUID())"`).
4. **SMTP** (optional — only needed to email codes instead of copy-pasting them).
   See `.env.example`.
5. Add all of the above to Vercel → Settings → Environment Variables, and
   redeploy so the functions pick them up.

### Using it

- **New client** — one button. A code is generated immediately so there is
  nothing to think about.
- **Delivery folder** — "Choose / create folder" opens a Drive folder browser
  right there in the admin — pick an existing shoot folder, or create a
  brand-new one, without ever leaving the page. (A raw folder-ID paste box is
  still there as a fallback for folders made by hand.)
- **Share folder** — turns on link-sharing for that one folder only.
- **Send it** — copy the pre-written WhatsApp message, copy just the link, or
  hit **"Email this to them"** to send the same message over SMTP.
- **Revoke** — turns sharing back off; the code stops working immediately.
- Deleting a client never touches the photos in Drive.

### Security notes

- One password, held in an env var, exchanged for an HMAC-signed httpOnly cookie
  (7 days). The cookie is signed, not encrypted — it carries only an expiry.
- Every endpoint except sign-in requires that cookie.
- There is **no rate limiting** on the sign-in endpoint beyond a fixed delay on a
  wrong password. If the URL ever gets out, a long password is what protects it.

## Client downloads (`/client`)

### How the client gets their photos

1. Opens the link Viraj sent (`/client/<code>`, or `/client` and types the code)
2. Sees their name, the shoot, and the photo count
3. If the shoot is under the ZIP size cap (see below): a **Download ZIP** button that
   streams the zip straight from the site, plus an **Open in Google Drive** link alongside
   it as an alternative. Over the cap: only the Drive link, with a note explaining why —
   Drive handles arbitrarily large folders fine, just not proxied through our own server.

No account, no signup. There is also a quiet **Client area** link in the footer
for anyone who loses the message.

### The ZIP download cap

`api/download.js` builds the zip inside one serverless function call, streaming each
Drive file straight into the response rather than buffering the whole archive — but that
call still has a time limit (`vercel.json` sets `maxDuration: 60` for it, capped by
whatever the Vercel plan actually allows). `DOWNLOAD_MAX_FILES` / `DOWNLOAD_MAX_MB` in
`.env.example` (defaults 400 files / 1500 MB) decide when a shoot is too big to risk it;
`/api/client` and `/api/download` both read the same numbers so the button shown to the
client always matches what the download endpoint will actually do.

### Why the site does the sharing

Viraj never opens Drive's share dialog, because that dialog is where the
expensive mistake lives: sharing a **parent** folder exposes every client inside
it. The site sets the permission on exactly the folder id the gallery points at,
as **Viewer** — the client can open and download, but cannot rename, delete,
upload, or see anything outside that folder.

**Revoke** removes that permission again, killing the link for good.

For this the service account needs **Editor** on the delivery folders, with
"Editors can change permissions" left on — the same Editor access
`DRIVE_ROOT_FOLDER_ID` already needs for folder creation (see the warning in
`.env.example`: that root folder must **never** be set to "Anyone with the
link", since Drive link-sharing is inherited by children and would make every
newly created delivery folder public by default).

### What this does and does not protect

- The code gates the *front door*. Codes are the shoot title + 6 random
  characters from a 31-letter alphabet, and the lookup endpoint is
  rate-limited to 10 tries a minute per IP.
- Once a client has the **Drive** link, forwarding it still works — the code
  protects discovery, not the folder. Revoke is the answer if that happens.
- `/client` is `noindex` and never cached.
- Anyone who can download can also copy to their own Drive. Those are the same
  permission in Drive; it cannot be split.

## Customising for the client

Content is realistic **placeholder** — search for these to swap in the real
details:

All content lives in `src/data.js`:

| Where | What to replace |
| ----- | --------------- |
| `P` | name, role, email, city |
| `FRAMES` | work-card copy — normally comes from Contentful (see **Photos from Contentful**); the array here is the placeholder fallback |
| `ABOUT` | About-page bio, approach, timeline (portrait image comes from Contentful) |
| `METRICS`, `QUOTES`, `SHOTLIST` | numbers, testimonials, services |
| Photos | pulled from Contentful at build time — see **Photos from Contentful**. Without credentials the site falls back to `picsum.photos` placeholders |
| `THEME` | the single fixed accent/base palette (no switcher — see above) |
| `index.html` | `<title>`, description, and OG/Twitter tags (marked `TODO(client)`) |
| `public/favicon.svg`, `public/og.svg` | the lens mark + social card (export `og.svg` to a 1200×630 PNG for Facebook/Twitter) |

The 3D hero lives in `src/HeroCanvas.jsx` — particle count, size, and drift
speed are all tunable constants at the top of the file.
